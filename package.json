{
  "name": "tacfit-home",
  "version": "1.0.0",
  "description": "Offline military-style bodyweight workouts, tailored to you.",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "sync:web": "rsync -a --delete index.html manifest.json sw.js css js icons www/",
    "cap:sync": "npm run sync:web && cap sync",
    "cap:android": "npm run sync:web && cap open android",
    "cap:ios": "npm run sync:web && cap open ios",
    "electron:start": "npm run sync:web && electron .",
    "electron:build:win": "npm run sync:web && electron-packager . TacFitHome --platform=win32 --arch=x64 --icon=electron/icon.ico --out=dist --overwrite --app-copyright=\"© 2026 BillScrub LLC\" --win32metadata.CompanyName=\"BillScrub LLC\" --win32metadata.ProductName=\"TacFit Home\" --ignore=\"^/(android|ios|assets|supabase|design|\\.git)($|/)\""
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@electron/packager": "^20.3.0",
    "electron": "^33.2.0"
  }
}
